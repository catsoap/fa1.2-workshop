{
  "name": "@fa1.2-workshop/monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "cd packages/contracts && npm run compile && cd -",
    "migrate": "cd packages/contracts && npm run migrate && cd -",
    "start-sandbox": "cd packages/contracts && npm run start-sandbox",
    "start-dapp": "cd packages/dapp && npm start",
    "test-contracts": "cd packages/contracts && npm run test && cd -"
  },
  "workspaces": [
    "./packages/*"
  ]
}
