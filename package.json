{
  "name": "@fa1.2-workshop/monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "cd contracts && npm run compile && cd -",
    "migrate": "cd contracts && npm run migrate && cd -",
    "start-sandbox": "cd contracts && npm run start-sandbox",
    "start-dapp": "cd dapp && npm run start",
    "test-contracts": "cd contracts && npm run test && cd -"
  },
  "workspaces": [
    "dapp",
    "contracts"
  ],
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "parcel": "^1.12.4"
  }
}
